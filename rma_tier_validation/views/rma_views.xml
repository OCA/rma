<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright (C) 2022 Open Source Integrators
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="view_rma_search" model="ir.ui.view">
        <field name="name">rma.tier.validation.search</field>
        <field name="model">rma</field>
        <field name="inherit_id" ref="rma.rma_view_search" />
        <field name="arch" type="xml">
            <filter name="no_user_id_filter" position="after">
                <separator />
                <filter
                    name="needs_review"
                    string="Needs my Review"
                    domain="[('reviewer_ids','in',uid), ('state', 'not in', ['finished', 'locked', 'cancelled'])]"
                    help="My RMAs to review"
                />
                <filter
                    name="tier_validated"
                    string="Validated"
                    domain="[('validated', '=', True)]"
                    help="RMAs validated and ready to be confirmed"
                />
            </filter>
        </field>
    </record>
</odoo>
